[project]
name = "projeto-ciencia-de-dados"
version = "1.0.0"
requires-python = ">=3.11"

dependencies = [
    "pandas",
    "numpy",
    "matplotlib",
    "scikit-learn",
    "mlflow",
    "joblib",
    "fastapi",
    "uvicorn"
]
